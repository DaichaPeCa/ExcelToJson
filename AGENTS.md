# AGENTS.md

## プロジェクトの目的

このリポジトリでは、Windows向けCLIツール `ExcelToJson` を実装する。

製品仕様の正本は `docs/REQUIREMENTS.md` である。実装、テスト、READMEは必ず同文書と整合させること。

## 仕様の境界

- `docs/REQUIREMENTS.md` に明記された動作を変更しない。
- 仕様にない振る舞いを、一般論や実装上の都合だけで追加しない。
- 複数の妥当な動作があり、選択によって外部仕様が変わる場合は、実装前に論点、選択肢、影響を提示して確認する。
- 実装詳細として閉じる判断は、要件への適合、単純さ、保守性、型安全性を基準に決め、READMEまたはコード上に理由を残す。
- `docs/REQUIREMENTS.md` の「本書で確定していない事項」を必要とする段階では、独断で製品仕様を固定しない。

## 最初に行うこと

1. `docs/REQUIREMENTS.md` と `README.md` を全文読む。
2. 現在のファイル構成と変更状態を確認する。
3. 実装前に、次を簡潔に提示する。
   - 採用する.NETバージョン候補と理由
   - プロジェクト / クラス構成
   - 各責務と依存方向
   - Excel読み取り、変換、参照解決、JSON書き込みの境界
   - テスト方針
   - 技術検証が必要な数式セルの扱い
   - 仕様上の未解決点があれば、その内容
4. 外部仕様が分岐する未解決点がなければ、合意した設計に沿って実装と検証を完了する。

## 設計方針

小規模CLIに不必要な多層化を持ち込まず、次の責務は明確に分離する。

- CLI境界: 引数検証、終了コード、利用者向けエラー表示
- Workbook読み取り: ClosedXML固有APIから、変換に必要なブック表現への変換
- 設定解析: `setting` の読み取り、正規化、検証
- スキーマ解析: ヘッダー、メモ、型指定、シート定義の検証
- 値変換: `text` / `number` / `boolean` / `date` の型変換
- 参照解決: `object` / `array`、件数検証、多段参照、循環検出
- JSON出力: System.Text.JsonによるUTF-8・インデント付き出力と安全な置換

### 依存関係

- ClosedXML固有型を、変換規則や参照解決の中心へ漏らさない。
- System.Text.Json固有の書き込み処理を、Excel読み取りや設定解析へ漏らさない。
- 制御情報と通常データを型または明示的な値オブジェクトで区別する。
- 大文字小文字を区別しない識別子と、区別するID / JSONプロパティ名を、同じ比較規則で扱わない。
- 生の文字列を処理の各所で再解析せず、設定、列型、参照先、日付 / 日時区分を解析時に型として確定し、後段へ渡す。
- 例外を通常の分岐に乱用せず、入力不正には位置情報を含む検証結果またはドメイン固有例外を用いる。

### 規模に関する判断

- DDD / Clean Architectureの考え方は責務境界と依存方向に使う。
- V1を形式的な多プロジェクト構成へ分割すること自体を目的にしない。
- 将来要件を推測した抽象化、汎用フレームワーク、CLIオプションを追加しない。
- ClosedXMLとSystem.Text.Json以外の本番依存関係を追加する場合は、必要性と代替案を先に説明する。

## 実装上の重要事項

- 制御情報の比較には、仕様どおり大文字小文字を区別しない比較を明示的に使用する。
- ID、参照ID、JSONプロパティ名の比較には、仕様どおり大文字小文字を区別する比較を使用する。
- CurrentCulture依存処理をテスト可能にする。テストでカルチャを変更する場合は、必ず元へ戻し、並列実行による干渉を避ける。
- 日付値と「元入力に時刻要素があったか」を一つのモデルとして保持する。
- 循環検出は、エラーに参照経路を含められる構造にする。
- 出力先へ直接書き始めず、変換とシリアライズが成功した後に既存ファイルを安全に置換する。
- 数式については、ClosedXMLの挙動を小さな検証用ブックで確認し、取得不能時に仕様どおりエラーへできることをテストする。

## テスト方針

- `docs/REQUIREMENTS.md` 15章の受け入れ条件を、自動テストへ対応付ける。
- 正常系だけでなく、13章のエラー条件を個別に検証する。
- Workbookを読む統合テストと、変換規則をExcel非依存で検証する単体テストを分ける。
- 大文字小文字、Trim、空行終端、重複ID、重複プロパティ名を境界値として扱う。
- 日付は、日付のみ、時刻あり、`00:00:00`、独自入力形式、独自出力形式を検証する。
- 参照は0件、1件、複数件、多段、自己循環、間接循環、`_` シート参照を検証する。
- CLIは出力パス、上書き、失敗時の既存ファイル保護、終了コードを検証する。
- テスト用Excelファイルをバイナリで固定する場合は、生成元と意図が追跡できるようにする。可能ならテスト内で最小ブックを生成する。

## 完了条件

作業完了と報告する前に、少なくとも次を満たすこと。

- ビルドが成功する
- 全自動テストが成功する
- Release publishが成功する
- Windows向けSelf-contained / Single-file成果物を確認する
- 成果物だけで、.NET RuntimeとExcelを別途導入せずに実行できる構成を確認する
- `README.md` に確定したビルド、テスト、実行、publish手順を反映する
- `docs/REQUIREMENTS.md` と実装の差分がないことを確認する
- 未解決事項、技術的制約、検証できなかった点があれば明示する

## 変更時の報告

最終報告では、次を簡潔に示す。

- 実装した責務と主要な設計判断
- 変更したファイル
- 実行した検証と結果
- 既知の制約または残課題

ユーザーの明示的な依頼なしに、コミット、push、リリース公開を行わない。
